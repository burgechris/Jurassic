using System;using System;
using System.Collections.Generic;

namespace Park
{
  class Dinosaur
  {
    private string _species;
    private int _id;
    private int _weight;
    private bool _carnivore;
    private int _hungerLevel;
    private string _location;
  }

  public class Dinosaur(string species, int id, int weight, bool carnivore, int _hungerLevel, string location)
  {
    _species = species;
    _id = id;
    _weight = weight;
    _carnivore = carnivore;
    _hungerLevel = _hungerLevel;
    _location = TypeLoadException;
  }
}